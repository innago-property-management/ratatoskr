# Delphi Review — provider-generalization

**Date:** 2026-03-30
**Panel ID:** panel-7e9d8429
**Panelists:** systems_architect, skeptic, pragmatist, advocate, user_experience

---

## Verdict

Unanimously approved. Approach C is correct for both items.

## Consolidated Findings

| # | Finding | Source(s) | Severity | Resolution |
|---|---------|-----------|----------|------------|
| S-1 | `timeout_ms` has no injection point in `LLMProvider.complete()`. Factory must bake timeout into provider HTTP client at construction. | Skeptic, Architect, Pragmatist | **High** | Factory injects `httpx.Timeout` at provider construction. `complete()` interface unchanged. AC-13 added. |
| S-2 | `max_output_tokens` -> `max_tokens` mapping not explicit in spec. | Skeptic, Pragmatist | Low | Made explicit: `reduce()` passes `max_tokens=self.max_output_tokens`. AC-14 added. |
| S-3 | Provider response format variance — verify fence-stripping works for all providers. | Architect | Low | Existing fence-strip logic covers this. Test case added. AC-15 added. |
| U-1 | No startup log for `PROFILE=local` — users won't know what profile set. | UX | Medium | Log at INFO level with effective overridden settings. AC-16 added. |
| U-2 | No `--profile` CLI flag — only env var, less discoverable. | UX | Medium | Add `--profile` to argparse, same pattern as `--provider`. AC-17 added. |
| U-3 | Pydantic error for invalid PROFILE is cryptic. | UX | Low | Custom validator error message in `model_validator`. AC-18 added. |

## Per-Panelist Highlights

### Systems Architect
- "Architecturally sound. Zero new abstractions."
- Flagged `complete()` contract verification across providers
- Confirmed `model_validator(mode="before")` is correct pydantic pattern for profile injection

### Skeptic
- Primary contributor to S-1 (timeout gap) — the highest-severity finding
- Recommended factory-baked timeout (Option B) over `complete()` interface change
- Noted `LLMResponse.content` may be `None` for tool-call-only responses (guard recommended)

### Pragmatist
- "Solid spec, mostly practical"
- Confirmed approach C avoids over-engineering
- Flagged `max_output_tokens` flow clarity (S-2)

### Advocate
- "This is one of those rare features where the design is obviously correct in hindsight"
- Called current `import anthropic` in reducer.py an "architectural lie" given polyglot-LLM value prop
- Strong endorsement of user stories 1 and 3

### UX Advocate
- "Strong user-centered design"
- Contributed all three U-findings (startup log, CLI flag, error message)
- Emphasized `--profile` discoverability alongside existing `--provider` flag
