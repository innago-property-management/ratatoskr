# Requires Kubernetes >= 1.21 (policy/v1 PDB)
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: ratatoskr
  labels:
    app.kubernetes.io/name: ratatoskr
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: ratatoskr
